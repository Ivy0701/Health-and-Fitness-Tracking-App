# Dev proxy target — must match backend PORT (backend/.env). Default in vite.config is 5000.
VITE_BACKEND_ORIGIN=http://127.0.0.1:5000

# Optional: full API base (skips proxy). Example: http://localhost:5001/api
# VITE_API_BASE_URL=
